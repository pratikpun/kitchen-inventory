line-length = 100
target-version = "py312"

[lint]
select = [
    "E",   # style errors
    "F",   # pyflakes (unused imports, variables, etc.)
    "I",   # import sorting
    "W",   # style warnings
]

[format]
quote-style = "double"